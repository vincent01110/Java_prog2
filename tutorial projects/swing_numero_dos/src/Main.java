import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {

        /**
        JFrame frame = new JFrame(); // creates frame

        frame.setSize(420,420); // sets x and y dimensons of the frame
        frame.setTitle("JFrame title goes here"); // sets frame title
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // the corner X button exits app
        frame.setResizable(true); // prevent user from resizing the window

        ImageIcon image = new ImageIcon("astr.png"); // creates ImageIcon object
        frame.setIconImage(image.getImage()); // change icon of frame

        frame.getContentPane().setBackground(new Color(0xFCE300)); // changes color of background

        frame.setVisible(true); // make frame visible */

        //MyFrame myFrame = new MyFrame();
        new MyFrame();

    }
}